\documentclass{rapport}
\usepackage{lipsum}
\usepackage{gensymb}
\usepackage{float}
\usepackage{graphicx} % Required for inserting images
\usepackage{listings}
\usepackage{xcolor}
\usepackage{minted}
\usepackage{amsmath, amssymb, amsfonts}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,        % Enable colored links
    linkcolor=black,         % Color for internal links
    citecolor=black,         % Color for citations
    filecolor=blue,         % Color for file links
    urlcolor=blue           % Color for URLs
}

\title{file title} %title of the file

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,         
    breaklines=true,                 
    captionpos=b,                    
    keepspaces=true,                 
    numbers=left,                    
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2
}

\lstset{style=mystyle}

\begin{document}

%----------- Report information ---------
\logo{logos/uoft_logo.png}
\uni{\textbf{University of Toronto}}
\ttitle{AER1515 Perception for Robotics: Assignment 2} %title of the file
\subject{AER1515} % Subject name
\topic{Assignment 2} % Topic name

\students{\textsc{Yang-Chen Lin}} % information related to the students

%----------- Init -------------------
        
\buildmargins % display margins
\buildcover % create the front cover of the document
\toc % creates the table of contents

%------------ Report body ----------------
\section{Answers}
\subsection{Question 1: Feature Point Detection and Corresondences}
\subsubsection{Feature Detection}
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q1.1.1.png}
    \caption{Detected keypoints of left-test-images by the R2D2 framework (unified circle radius)}
    \label{fig:1}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q1.1.2.png}
    \caption{Detected keypoints of left-test-images by the R2D2 framework (unified circle radius)}
    \label{fig:2}
\end{figure}

I utilized the R2D2 framework as the feature detection algorithm. 
From Fig.~\ref{fig:1} and Fig.~\ref{fig:2}, it can be observed that most keypoints are 
concentrated around visually distinctive regions of the image, such as object boundaries, 
textured surfaces, and high-contrast areas. The clustering of keypoints occurs because R2D2 
detects features across multiple scales, which can lead to overlapping detections when strong 
structures are present at several pyramid levels. In the following plots, the radius of 
each circle represents the detected scale of the corresponding keypoint, illustrating the multi-scale 
nature of the detector.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q1.1.1_scaled.png}
    \caption{30 keypoints with scaled radius}
    \label{fig:3}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q1.1.2_scaled.png}
    \caption{30 keypoints with scaled radius}
    \label{fig:4}
\end{figure}

The radius of each green circle represents the scale predicted by R2D2 for that keypoint, corresponding to the 
size of the image region that contributes to its descriptor. Larger circles indicate coarse-scale features 
that remain stable across viewpoint and blur changes, while smaller circles 
correspond to fine-scale texture details. The variation in circle sizes illustrates the multi-scale nature of 
the detector, allowing robust matching across different levels of zoom and scene detail.
Since the R2D2 framework natively provide "scores" for each keypoint, it's easy to identify which keypoints
are less reliable when we perform image matching in the later steps. 

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q1.1.1_viz.png}
    \caption{For one given input image (1st row), the repeatability (2nd row) and reliability
heatmaps (3rd row) extracted at a single scale. Top-scoring keypoints are shown as
    green crosses in the first image. (Plot generated by visualization script from the \href{https://github.com/naver/r2d2}{R2D2 repository}.)}
    \label{fig:5}
\end{figure}

\subsubsection{Feature Matching}

The feature matching pipeline used in this part is implemented across three scripts in the repository:
	\texttt{tools/R2D2.py}, \texttt{tools/matcher.py}, and \texttt{starter\_code\_feature.py}. The following summarizes the
implementation, parameters, and the filtering rules applied during matching and depth computation.

\paragraph{Overview}
\begin{itemize}
    \item \texttt{tools/R2D2.py:} performs feature detection and descriptor extraction using the R2D2 model. It produces
        per-image files containing \texttt{keypoints} (pixel coordinates and scale), \texttt{descriptors} (128-d vectors), and
        \texttt{scores} (reliability values).
    \item \texttt{tools/matcher.py:} implements brute-force L2 nearest-neighbour matching with Lowe's ratio test and simple
        geometric constraints (vertical epipolar tolerance and disparity sign).
    \item \texttt{starter\_code\_feature.py:} perfroms feature extraction and matching, computes disparity and depth using
        stereo calibration, writes the sparse depth predictions to \texttt{P3\_result.txt}, and
        generates visualizations.
    \item \texttt{tools/calculate\_depth\_error.py:} calculate depth errors from the ground truth depth for training images.
    
\end{itemize}

\paragraph{Feature extraction (\texttt{tools/R2D2.py})}
The R2D2 extractor is invoked (via \texttt{r2d2/extract.py}) to produce a sidecar \texttt{.r2d2} file per image. Each
file stores arrays loaded with NumPy:
$ \text{keypoints}\in\mathbb{R}^{N\times 3},\quad\text{descriptors}\in\mathbb{R}^{N\times 128},\quad\text{scores}\in\mathbb{R}^N.$
Keypoints are in pixel coordinates $(u,v)$ and include a scale value.

\paragraph{Descriptor matching (\texttt{tools/matcher.py})}
Let $L$ denote left descriptors and $R$ denote right descriptors. Matching proceeds as follows:
\begin{enumerate}
    \item Compute the pairwise L2 distance matrix $D\in\mathbb{R}^{|L|\times|R|}$ with entries $D_{ij}=\lVert d^L_i-d^R_j\rVert_2$.
    \item For each left descriptor find the nearest and second-nearest right descriptors ($j_1, j_2$) and associated distances
        $d_0=D_{i,j_1}$ and $d_1=D_{i,j_2}$.
    \item Apply Lowe's ratio test and a distance threshold:
        \[\text{ratio\_mask}_i = (d_0/d_1 < \tau_{\mathrm{ratio}}) \;\wedge\; (d_0 \le \tau_{\mathrm{dist}}).\]
        Default code parameters: $\tau_{\mathrm{ratio}}=0.9$, $\tau_{\mathrm{dist}}=5.0$.
    \item Enforce an epipolar constraint: $|v^L_i - v^R_{j_1}| < \epsilon_y$ with $\epsilon_y=2$.
    \item Filter out negative disparities: $u^R_{j_1} < u^L_i$.
    \item The final mask is the conjunction of the ratio, epipolar and disparity masks; matches and their descriptor distances
        are returned as arrays.
\end{enumerate}

\paragraph{Disparity and depth (\texttt{starter\_code\_feature.py})}
For each accepted match (left index $i$, right index $j$) the pixel coordinates are extracted and disparity is computed as
\[\text{disparity} = u_L - u_R.\]
Depth is then estimated from stereo geometry via
\[ z = \frac{f\cdot B}{\text{disparity}},\]
where $f$ is the focal length and $B$ is the baseline obtained by factorizing the projection matrices
(from the classes: \texttt{krt\_from\_p} and \texttt{get\_stereo\_calibration}).

\paragraph{Filtering rules}
As stated in the handout, to avoid invalid or very noisy depth estimates, the implementation applies conservative filters:
\begin{itemize}
    \item Skip matches with disparity $\le 0.1$ (prevents division by zero).
    \item Discard depths $z>80\,$m or $z<0$ (long-range predictions are unreliable for evaluation).
\end{itemize}

\paragraph{Output and visualization}
Filtered sparse depth predictions are written to \texttt{P3\_result.txt} with the format
	\texttt{(image\_id u v disparity depth)}. Visualizations are created using OpenCV's \texttt{cv.drawMatches}.

\paragraph{Evaluation of Estimated Depth Error Using Epipolar Constraint on Training Set}
 
Parameters used: \texttt{distance\_ratio:0.82, distance\_threshold:2.0}
    \begin{itemize}
        \item Mean Absolute Error (MAE): 1.3131
        \item Root Mean Squared Error (RMSE): 2.9181
        \item Relative Error: 0.0575
    \end{itemize}


\vspace{2mm}

\begin{figure}[H]
        \centering
        \includegraphics[width=\textwidth]{images/Q1.2.1.png}
        \caption{Feature matching on the test images using brute-force L2 nearest-neighbour matching and epipolar constraints.}
        \label{fig:6}
\end{figure}

\subsubsection{Outlier Rejection}

\paragraph{RANSAC-based Outlier Rejection}

To improve geometric consistency beyond the simple epipolar vertical-tolerance test, we run RANSAC to robustly estimate the fundamental matrix from tentative descriptor matches and keep only inliers. The added RANSAC stage performs the following steps:

\begin{enumerate}
    \item Build tentative matches using brute-force L2 nearest-neighbour matching with Lowe's ratio test.
    \item Convert matched pixel coordinates to homogeneous points:
    \[
      \mathbf{x}_1 = [u_1, v_1, 1]^T,\quad \mathbf{x}_2 = [u_2, v_2, 1]^T.
    \]
    \item Estimate the fundamental matrix $F$ using RANSAC with the \texttt{cv.findFundamentalMat()} \\ function:
    \[
      F = \texttt{findFundamentalMat}(\{\mathbf{x}_1\},\{\mathbf{x}_2\};\ \texttt{method}=\texttt{FM\_RANSAC},\ \tau_{\text{ransac}},\ \text{conf}).
    \]
    In the implementation the typical parameters are $\tau_{\text{ransac}}=1.0$ (pixel threshold) and $\text{conf}=0.99$.
    \item Use the returned inlier mask from RANSAC to filter matches for downstream disparity and depth computation.
\end{enumerate}

\paragraph{Evaluation of Estimated Depth Error Using RANSAC on Training Set}
Parameters used: \texttt{distance\_ratio:0.82, distance\_threshold:2.0, ransac\_reproj\_thershold:1, confidence:0.99}    
    \begin{itemize}
        \item Mean Absolute Error (MAE): 1.2295
        \item Root Mean Squared Error (RMSE): 2.7331
        \item Relative Error: 0.0563
    \end{itemize}

\vspace{2mm}

\begin{figure}[H]
        \centering
        \includegraphics[width=\textwidth]{images/Q1.3.1.png}
        \caption{Feature matching on the test images using brute-force L2 nearest-neighbour matching and RANSAC outlier rejection.}
        \label{fig:7}
\end{figure}

\subsubsection{Alternative Algorithm}
For alternative algorithms for image matching, two approaches will be implemented. First, I implemented an alternative pipeline that replaces the learned R2D2 detector 
with the classical ORB detector/descriptor. ORB produces binary descriptors (32 bytes per keypoint) and keypoints with fewer metadata fields than R2D2, so a small amount of adapter code is required
to feed ORB output into the same downstream matching and depth estimation pipeline. The main changes are summarised below.

\paragraph{ORB: Code-level adaptations}
The following steps describe the minimal adapter implemented in \texttt{starter\_code\_feature.py}:
\begin{enumerate}
    \item Detect and compute ORB keypoints and descriptors:
        	\texttt{kp, desc = orb.detectAndCompute(img, None)}.
    \item Convert the list of \texttt{cv2.KeyPoint} objects to a NumPy array of pixel coordinates:
        	\texttt{pts = np.array([k.pt for k in kp], dtype=float)} (shape $N\times 2$).
    \item Extract a per-keypoint score from ORB's \texttt{kp.response}.
    \item A Hamming-capable matching path was introduced so binary descriptors can be matched natively without converting them to floating point,
    if descriptors are in binary, use Hamming distance.
    \item Initialize \texttt{FeatureMatcher} with the adapted arrays: \texttt{FeatureMatcher(pts, desc, scores, ...)}.
\end{enumerate}

\paragraph{ORB: Expected outcome}
ORB is faster but typically less robust than the learned R2D2 features in challenging illumination or viewpoint changes.
Parameters used: \texttt{distance\_ratio:0.9, distance\_threshold:45.0, ransac\_reproj\_thershold:1, confidence:0.99}  
\begin{itemize}
        \item Mean Absolute Error (MAE): 2.4556
        \item Root Mean Squared Error (RMSE): 4.6676
        \item Relative Error: 0.0795
\end{itemize}

\begin{figure}[H]
        \centering
        \includegraphics[width=\textwidth]{images/Q1.4.1.png}
        \caption{Feature matching on the test images using brute-force nearest-neighbour matching and RANSAC outlier rejection with ORB.}
        \label{fig:8}
\end{figure}

\noindent The second approach is to use a fully learned Graph-based neural-network framework \href{https://github.com/magicleap/SuperGluePretrainedNetwork}{SuperGlue},
 that combines with an Optimal Matching layer that is trained to perform matching on two sets of sparse image features. In this network, it natively
 uses the output from \href{https://github.com/rpautrat/SuperPoint}{SuperPoint}, and to simplify the process, I'll use the demo script in SuperGlue's repository
 to produce keypoint pairs and descriptors.

 \paragraph{SuperGlue: Code-level adaptations}

 \begin{itemize}
        \item Mean Absolute Error (MAE): 1.8
        \item Root Mean Squared Error (RMSE): 4.3443
        \item Relative Error: 0.0861
\end{itemize}

\begin{figure}[H]
        \centering
        \includegraphics[width=\textwidth]{images/Q1.4.2.png}
        \caption{Feature matching on the test images using the SuperGlue framework.}
        \label{fig:9}
\end{figure}

\subsection{Question 2: 3D Point Cloud Registration}
\subsubsection{Nearest Neighbour Search}
\begin{figure}[H]
        \centering
        \includegraphics[width=0.5\textwidth]{images/combined_icp_mean_distance.png}
        \caption{Feature matching on the test images using the SuperGlue framework.}
        \label{fig:10}
\end{figure}


\subsubsection{Pose Estimation from the Correspondences}
\begin{figure}[H]
        \centering
        \includegraphics[width=0.5\textwidth]{images/combined_icp_translation_components.png}
        \caption{Feature matching on the test images using the SuperGlue framework.}
        \label{fig:11}
\end{figure}

\subsubsection{Iterative Closest Point Registration}
\begin{figure}[H]
        \centering
        \includegraphics[width=\textwidth]{images/combined_before_after.png}
        \caption{Feature matching on the test images using the SuperGlue framework.}
        \label{fig:12}
\end{figure}

\begin{listing}[H]
    \caption{Estimated rotation and translation errors on training sets, and estimated test set 6D pose}
    \begin{minted}[]{text}
    bunny -- Rotation error (deg): 0.0000, Translation error: 0.0001
    dragon -- Rotation error (deg): 0.0000, Translation error: 0.0000

    armadillo -- Estimated 6D pose:
    [[ 0.777541 -0.066073  0.625351 -2.689722]
    [-0.384049  0.737557  0.555442 -1.858343]
    [-0.497931 -0.672045  0.548106 -5.961925]
    [ 0.        0.        0.        1.      ]]
    \end{minted}
\end{listing}

\end{document}
